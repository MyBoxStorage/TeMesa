# Achata o node_modules (necessário no Windows com Turbopack + Tailwind v4)
# Sem isso, o Turbopack falha em resolver @import "tailwindcss" via pnpm virtual store
shamefully-hoist=true

# Suprime warnings de build de pacotes nativos
ignore-scripts[]=sharp
ignore-scripts[]=unrs-resolver
